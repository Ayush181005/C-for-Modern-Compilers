# include <stdio.h>

void main(){
    // Check whether 97 is divisible by 3 or not
    int num = 97;
    if(num%3==0){
        printf("%d is divisible by 3", num);
    }
    else{
        printf("%d is not divisible by 3", num);
    }
}